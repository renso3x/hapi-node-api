'use strict';
module.exports = {
  up: (queryInterface, Sequelize) => {
    return new Promise(resolve => {
      resolve();
    })
  },
  down: (queryInterface, Sequelize) => {
    return new Promise(resolve => {
      resolve();
    })
  }
};